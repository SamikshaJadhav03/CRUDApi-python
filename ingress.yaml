apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crud-api-ingress6
spec:
  rules:
  - http:
      paths:
      - path: /books
        pathType: Prefix
        backend:
          service:
            name: crud-api-service6
            port:
              number: 5000