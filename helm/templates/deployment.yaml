apiVersion: apps/v1
kind: Deployment
metadata:
  name: crud-api-deployment2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: crud-api
  template:
    metadata:
      labels:
        app: crud-api
    spec:
      containers:
        - name: crud-api-container
          image: terraformrg.azurecr.io/my-api-image:${{ github.sha }}
          ports:
            - containerPort: 5000